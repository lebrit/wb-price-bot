# WB Price Bot

Self-hosted Telegram-бот для отслеживания цен Wildberries. Поддерживает много товаров и
несколько разрешённых пользователей, уведомления по проценту, сумме или целевой цене,
историю изменений, наличие и экспериментальную персональную цену своего WB-аккаунта.

Текущая версия: `0.3.2`.

## Что уже работает

- добавление товара по полной ссылке, короткой ссылке WB или артикулу;
- отдельный список товаров каждого Telegram-пользователя;
- условия «подешевеет на X%», «подешевеет на X ₽» и «станет не дороже X ₽»;
- несколько независимых правил на один товар;
- выбор и последующая смена конкретного размера с закреплением текущего продавца;
- индивидуальный регион каждого пользователя по Telegram-геопозиции;
- тихие часы для всех уведомлений и настраиваемая дневная сводка;
- PNG-графики цены за 7, 30 и 90 дней;
- массовый импорт до 50 ссылок/артикулов и экспорт CSV/JSON;
- папки, теги и фильтры `/folder` / `/tag`;
- уведомление при возвращении товара в наличие;
- история только реальных изменений цены/наличия;
- надёжная очередь уведомлений в SQLite с повторной отправкой после ошибок Telegram;
- пауза, удаление и ручная проверка отдельного товара;
- пакетная проверка публичных цен, ограничение частоты и случайный разброс времени;
- явная подпись источника: публичная цена, цена аккаунта или цена с WB Кошельком;
- зашифрованное хранение браузерной сессии аккаунта (Fernet);
- подключение аккаунта из Telegram Mini App без терминала и передачи файла сессии;
- временный изолированный Chromium, одноразовое окно входа и очередь браузерных слотов;
- многопользовательский доступ: заявки, одобрение/блокировка администратором и три режима регистрации;
- админ-панель в Telegram с CPU, load average, RAM, swap, диском, uptime и статистикой сервиса;
- опциональный лицензированный fallback MPSTATS с контролем свежести данных;
- интерактивное русское меню установки, обновления, управления, диагностики,
  резервного копирования и удаления;
- отдельные непривилегированные контейнеры bot/auth и Caddy с автоматическим HTTPS;
- зафиксированные версии runtime-зависимостей в `requirements.lock`;
- SQLite в WAL-режиме и согласованные резервные копии через SQLite Backup API.

## Важное ограничение Wildberries

У Wildberries нет документированного API покупательских и персональных цен: официальный
[WB API](https://dev.wildberries.ru/en/openapi/api-information?locale=ru) предназначен
продавцам. Публичный адаптер использует недокументированный endpoint витрины, а персональный
режим — обычную карточку товара в авторизованном Chromium.

Действующая [оферта Wildberries для покупателей](https://legal.wildberries.ru/consumers-offer/country/ru/lang/ru/)
прямо запрещает использовать программы, алгоритмы, скрипты, краулеры и ботов для сбора
информации, включая цены и наличие, и допускает блокировку кабинета. Консервативный интервал
не устраняет это нарушение. Для эксплуатации нужен письменный допуск Wildberries или
лицензированный источник данных; без него проект следует считать исследовательским
прототипом с юридическим риском. Endpoint может измениться, начать отвечать `403/429/498`, а
сессия — истечь. Проект не обходит CAPTCHA и другие проверки и не выполняет покупки,
операции с корзиной или платежами.

Цена в уведомлении не гарантирует итоговую сумму заказа: на неё влияют регион, размер,
продавец, логистика, способ оплаты, WB Кошелёк и активные акции.

## Быстрая установка на Ubuntu/Debian

Нужно заранее:

1. Создать бота у [@BotFather](https://t.me/BotFather) и получить токен.
2. Узнать числовой Telegram ID владельца.
3. Создать домен или поддомен, например `auth.example.com`, и направить его A/AAAA-запись
   на сервер. Входящие порты `80/tcp`, `443/tcp` и при необходимости `443/udp` должны быть
   доступны для Caddy и выпуска сертификата.

Одна команда открывает интерактивное меню с пунктом «Установить»:

```bash
(umask 077; S=$(mktemp); trap 'rm -f "$S"' EXIT; curl -fsSL https://raw.githubusercontent.com/lebrit/wb-price-bot/v0.3.2/scripts/install.sh -o "$S" && bash -n "$S" && sudo bash "$S")
```

Установщик сам:

- поставит Docker/Compose из системного репозитория, если их нет;
- спросит Telegram token, ID администраторов, интервал, региональный `dest`, домен,
  режим регистрации и число одновременных окон входа;
- создаст отдельные каталоги данных и секретов;
- соберёт контейнеры с Chromium и настроит Caddy с автоматическим HTTPS;
- проверит Telegram и дождётся здорового состояния сервиса;
- установит команду `wb-price-bot`.

Полный upgrade ОС, перезагрузка, изменение firewall и удаление чужих Docker-ресурсов не
выполняются. Compose использует порты `80/443`; если они заняты другим proxy, установка
остановится и выполнит rollback.

При обновлении с `v0.2.x` старое меню сначала запускает совместимый локальный auth-адрес.
После обновления выйдите из меню, снова выполните `sudo wb-price-bot`, откройте пункт 11 и
укажите публичный домен. При запуске установщика `v0.3.2` командой выше домен запрашивается
сразу.

После установки:

```bash
sudo wb-price-bot
```

## Интерактивное меню

Главное меню содержит:

1. обновление до последнего GitHub Release с backup и rollback;
2. запуск;
3. остановку;
4. перезапуск;
5. состояние контейнера;
6. просмотр журнала;
7. замену/проверку Telegram token, ID администраторов и тестовое сообщение;
8. инструкцию, импорт и удаление WB-сессии;
9. интервал мониторинга, региональный `dest` и немедленную проверку;
10. добавление, проверку и удаление лицензированного MPSTATS API token;
11. домен web-авторизации, режим регистрации, браузерные слоты и проверку HTTPS;
12. создание, просмотр и восстановление резервных копий;
13. диагностику Docker, SQLite, bot/auth/Caddy, HTTPS, диска и Telegram;
14. удаление только runtime либо полный purge с разными контрольными фразами.

Явные команды также доступны:

```bash
sudo wb-price-bot status
sudo wb-price-bot logs
sudo wb-price-bot backup
sudo wb-price-bot doctor
sudo wb-price-bot update
```

## Использование бота

Основные команды:

- `/add` — добавить товар;
- `/list` — товары, история, пауза, удаление и ручная проверка;
- `/account` — персональная цена своего аккаунта (beta);
- `/settings` — регион, тихие часы и дневная сводка;
- `/import` — массовый импорт ссылок или TXT;
- `/export` — выгрузка CSV/JSON без секретов;
- `/folders`, `/folder`, `/tag` — папки и теги;
- `/users pending|approved|blocked` — управление пользователями для администратора;
- `/status` — состояние мониторинга;
- `/cancel` — отменить текущий мастер.

Для порога по проценту или сумме цена при добавлении становится контрольной. Если товар
дорожает, контрольная цена повышается. Когда накопленное падение достигает порога, бот
уведомляет и начинает считать следующее падение от новой цены. Так постепенное снижение не
теряется и одинаковая цена не вызывает повторные сообщения.

## Подключение своего аккаунта Wildberries

1. Откройте в личном чате `/account` → «Подключить / обновить».
2. Подтвердите предупреждение и нажмите «Открыть защищённое окно WB».
3. В отображаемом окне Wildberries самостоятельно откройте вход, введите телефон, код и при
   необходимости пройдите CAPTCHA.
4. Нажмите «Сохранить вход». Бот проверит авторизованный запрос, зашифрует `storage_state`,
   привяжет его к вашему Telegram ID и уничтожит временный Chromium.

Mini App передаёт серверу подписанный Telegram `initData`; сервер проверяет подпись, возраст
данных, владельца одноразового окна и статус доступа. Окно действует 10 минут. Кадры браузера
и ввод не сохраняются и не пишутся в журнал, но проходят через ваш self-hosted auth-сервис —
пользователи должны доверять владельцу сервера. CAPTCHA проходит только человек; обхода нет.

Аварийный импорт `wb-session.json` по SSH и `scripts/capture_wb_session.py` оставлены для
администратора как резервный путь, но для обычного пользователя терминал больше не нужен.
Если проверка авторизации ломается, бот приостанавливает персональный режим и не подставляет
гостевую цену.

## Многопользовательский доступ

ID из `TELEGRAM_ALLOWED_USERS` считаются администраторами. По умолчанию включён режим
`approval`: новый человек отправляет `/start`, администратор получает заявку с кнопками
«Разрешить / Отклонить». У каждого пользователя отдельные товары, правила, регион,
уведомления и зашифрованная WB-сессия.

- `approval` — доступ только после решения администратора, рекомендуемый режим;
- `open` — любой пользователь автоматически допускается после `/start`;
- `allowlist` — работают только заранее указанные ID.

Режим меняется через `sudo wb-price-bot` → «Web-авторизация и пользователи».

Подробнее: [docs/WB_PROVIDER.md](docs/WB_PROVIDER.md) и [docs/SECURITY.md](docs/SECURITY.md).

## Данные и каталоги сервера

```text
/opt/wb-price-bot/releases/       неизменяемые релизы
/opt/wb-price-bot/current         активный релиз
/etc/wb-price-bot/config.env      несекретные настройки
/etc/wb-price-bot/secrets/        Telegram/MPSTATS token и ключ шифрования
/var/lib/wb-price-bot/            SQLite и внутренние backup
/var/backups/wb-price-bot/        операторские backup
/usr/local/bin/wb-price-bot       команда управления
```

Сертификаты Caddy находятся в именованных Docker volumes проекта. Runtime-only удаление их
сохраняет, полный purge удаляет вместе с остальными локальными данными проекта.

Telegram token и ключ сессии не включаются в переносимую копию базы. Для восстановления на
другом сервере ключ шифрования нужно переносить отдельно по защищённому каналу либо заново
подключать WB-аккаунт.

## Локальная разработка

```bash
python -m venv .venv
.venv\Scripts\activate
python -m pip install -e ".[dev]"
ruff check .
ruff format --check .
mypy
pytest
```

Локальный запуск:

```bash
set TELEGRAM_BOT_TOKEN=...
set SESSION_ENCRYPTION_KEY=...
set TELEGRAM_ALLOWED_USERS=123456789
set AUTH_PUBLIC_URL=https://auth.example.com
python -m wb_price_bot run
```

Ключ Fernet можно создать командой `python -m wb_price_bot generate-key`.

## Лицензированный fallback MPSTATS

MPSTATS документирует товарный API с ценой, продавцом и остатком. При наличии активной
подписки добавьте token через `sudo wb-price-bot` → «Лицензированный источник MPSTATS».
Fallback используется только если публичный запрос WB недоступен. Он не подменяет персональную
цену аккаунта и цену конкретного размера, а данные старше `MPSTATS_MAX_AGE_HOURS` отклоняются.

## Известные ограничения

- один артикул WB сейчас возвращает одного продавца; бот закрепляет его `supplierId`, а другой
  продавец обычно представлен отдельным артикулом;
- график строится по реально сохранённым изменениям, поэтому новому товару нужно минимум две точки;
- часовой пояс тихих часов и сводки задаётся `APP_TIMEZONE` (по умолчанию `Asia/Irkutsk`);
- account-режим и внутренние buyer endpoints WB остаются beta и могут измениться.

Архитектура и эксплуатация описаны в [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) и
[docs/OPERATIONS.md](docs/OPERATIONS.md).
